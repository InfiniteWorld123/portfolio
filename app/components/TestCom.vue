<script setup>
const checked = ref(false);
</script>

<template>
    <!-- <div class="card flex justify-center">
        <ToggleSwitch v-model="checked">
            <template #handle="{ checked }">
                <Icon :class="'!text-xs pi'" :name="checked ? 'ic:baseline-wb-sunny' : 'ic:baseline-mode-night'" />
            </template>
        </ToggleSwitch>
        <p class="ml-2">{{ checked ? 'Light' : 'Dark' }}</p>
    </div> -->
</template>
